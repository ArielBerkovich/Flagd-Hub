pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        id 'java'
        id 'io.spring.dependency-management' version '1.1.3'
    }
}

rootProject.name = 'flagd-hub-server'

include 'flagd-hub-api'
include 'swagger-schemas'

